# 017 · Pulido y QA transversal — Tareas

## A · Playground fuera de producción, `?dev` se queda

- [x] Guardar el interruptor `?playground` de `App.tsx` con `import.meta.env.DEV` para que la Playground (y con ella el nivel de prueba) desaparezca del build de producción. (As-built: `Playground` pasa de import estático a `lazy(() => import(...))` detrás de `import.meta.env.DEV ? ... : null` — el nivel de prueba en realidad ya no dependía de la Playground para salir del bundle, sino de un relleno muerto en `registry.ts` (`placeholderDefaults`, sobrescrito siempre por los 12 niveles reales desde hace features): se eliminó y el nivel de prueba se reubicó como arnés visible solo en `src/playground/LevelHostDemo.tsx`, cumpliendo la spec al pie de la letra.)
- [x] Verificar sobre `dist/` que no queda ni rastro de la Playground ni del nivel de prueba, y que `npm run dev` la sigue sirviendo entera. (Verificado: `grep` sobre `dist/assets/*.js` sin resultados para "Playground"/"TestLevel"; ya no existe el chunk `TestLevel-*.js` que sí aparecía en builds anteriores; bundle principal ~13KB más pequeño. `npm run dev` + Playwright: las 14 secciones de la Playground siguen presentes, incluida la nueva de LevelHost.)
- [x] Promover `?dev` a característica permanente: actualizar su línea en el roadmap (ya no se retira) y documentarlo en AGENTS.md; comprobar que funciona en el build de producción. (Verificado con `npm run preview` + Playwright: el botón "Saltar nivel (dev)" sigue operativo en el build real; `?playground` en cambio cae a la landing normal, contexto limpio.)

## B · Higiene del build

- [x] `build.sourcemap: false` explícito en `vite.config.ts` y verificar que `dist/` no contiene `.map` ni referencias a ellos.
- [x] Añadir a `tech-stack.md` la regla: los archivos de test viven en el repo y **nunca** en el bundle de producción.
- [x] Comprobación automatizada del build que falle si `dist/` contiene `.map`, `.test.` o restos de la Playground. (As-built: `spec/tools/verify-build-hygiene.mjs`, encadenado en `npm run build` — no es un paso aparte que se pueda olvidar. Probado a propósito: inyectar un `.map` y un `.js` con "Playground" dentro de `dist/` hace que el script falle con código de salida 1 y los señale; limpio, sale con 0.)
- [x] Revisar el desglose de chunks: bundle principal, `matter-*` compartido solo por los niveles 3 y 4, un chunk por nivel. (Verificado: 12 chunks `LevelNN-*.js` + `matter-*.js` (~85KB) + `usePointer-*.js` compartido + `prng-*.js` compartido + `index-*.js` principal (~212KB, bajó ~13KB al retirar la Playground del grafo). La única mención a "matter-" dentro de `index-*.js` es una entrada del manifiesto de precarga de Vite —nombres de fichero como texto—, no el código de matter.js duplicado.)

## C · Antiespoilers y consola

- [x] Test de indistinguibilidad en el DOM para el nivel 9 (casilla congelada), el 10 (ventana con el Agree antes y después del sorteo) y el 12 (`switchAt` y contador de clics fuera del DOM), al estilo del que ya existe en el 8. (As-built: en los tres casos el código ya cumplía por construcción — ningún nivel escribía nunca `data-*` ni distinguía la casilla/ventana especial más allá del botón visible — así que los tests nuevos son candados de regresión, no arreglos. Nivel 9: la misma casilla, congelada vs. sin congelar, produce clase idéntica en dos renders separados. Nivel 10: las 4 ventanas antes del sorteo son byte-idénticas (`innerHTML`); las 6 perdedoras tras el sorteo, también entre sí. Nivel 12: ningún atributo de ningún elemento contiene "switch"/"click"; el único `style` inline es `--progress`.)
- [x] Implementar el mensaje de consola (`consoleGreeting.ts`, `console.log` con `%c`, en el tono del juego, no en tests). (As-built: `src/app/consoleGreeting.ts`, llamado una única vez desde `main.tsx` — nunca se importa desde ningún componente que los tests rendericen, así que "no se ejecuta en tests" sale gratis del propio punto de entrada, sin ninguna comprobación de entorno. Texto en inglés fijo, sin `useT()` a propósito: no es UI del juego, es la misma excepción que el nivel de prueba.)
- [x] El mensaje revela `?dev` como puerta secreta para programadores. (Verificado con Playwright contra `npm run dev`: aparece estilado en la consola real al cargar la página.)

## D · Balanceo de dificultad

- [ ] ✋ **Sesión de Sofía**: partida completa sin `?dev`, de la landing a los créditos, anotando por nivel si el tiempo da, si las muertes se sienten justas y si el truco se entiende.
- [ ] Aplicar los ajustes acordados como constantes del GDD §14, actualizando GDD y tests en el mismo cambio (nunca cambios de mecánica).

## E · Audio

- [ ] Repaso nivel a nivel con las cuatro combinaciones de interruptores (música on/off × efectos on/off). Que si el usuario bloquea el teléfono, o cambia de página, o aplicación, la música se pare.
- [ ] Verificar que ninguna salida (victoria, derrota, X, recarga) deja audio sonando ni la música agachada — atención especial al ducking del nivel 11.
- [ ] Ajustar volúmenes relativos si algo desentona (música, positivo/negativo, `coin`, voz).

## F · Responsive y accesibilidad

- [x] Barrido con Playwright de landing, selección, los 12 niveles, modales y créditos en 375 / 480 / 768 / 1280 / 1920, con toques CDP en los móviles. (Verificado programáticamente en las ~20 pantallas/estados × 5 anchos —landing, sus 4 modales, selección, los 12 niveles vía `?dev`, créditos—: **cero desbordamientos horizontales** en cualquier combinación. Único aviso del barrido: el botón "Saltar nivel (dev)" queda por debajo de 44px en móvil — es intencional (tech-stack.md: "pequeño y discreto a propósito", no forma parte de la UI real del juego). Toques CDP reales (`page.touchscreen`, respaldado por `Input.dispatchTouchEvent`) confirmados en Empezar y en Saltar nivel a 375/480px. Capturas de pantalla a 375 y 1920px en landing/selección/créditos y en los niveles con tablero más denso (1, 3, 4, 6, 8, 10, 11, 12) revisadas visualmente, sin recortes ni solapes.)
- [x] Foco visible y navegación por teclado en landing, selección, modales y créditos; `aria-label` verificado en todos los botones de solo icono. (As-built: `IconButton` ya exige `label` por tipos —imposible usarlo sin nombre accesible—; auditoría automatizada con Playwright sobre landing + sus 4 modales + selección + créditos: 0 botones sin nombre accesible en las 8 pantallas. Ningún componente suprime el `outline` por defecto del navegador (`grep` sin resultados en todo `src/`) ni `reset.scss` lo toca, así que el anillo de foco nativo llega intacto a todos los botones; `CuteButton` además tiene su propio anillo personalizado. Confirmado con Tab real: el primer salto siempre aterriza en un `<button>` real con `outline`/`box-shadow` visible.)
- [x] Implementar `prefers-reduced-motion` en veredicto gigante, confeti y escritura del nivel 11 (el bloqueo de input del 11 se mantiene aunque el texto aparezca de golpe); **ninguna regla de juego cambia**. (As-built: `GiantVerdict`/`Confetti` se resuelven enteros en CSS —`animation-duration: 0.01ms !important` para el veredicto (el `animationend` que dispara `onDone` sigue disparándose, solo que casi al instante) y `display: none` para el confeti, que no tiene ningún desenlace de JS que sincronizar—. El nivel 11 necesitaba JS de verdad porque lo que cambia es QUÉ TEXTO se renderiza, no una animación: nuevo hook compartido `usePrefersReducedMotion` (`src/hooks/`, con test propio incluido el caso sin `matchMedia`, ausente en jsdom); `useTypewriter.ts` solo cambia `visibleText` a la frase completa desde el primer render — `state`/`done`/`unlocked` (el reloj real que bloquea No/Yes) no se tocan ni un milisegundo.)
- [x] Partida completa con `prefers-reduced-motion` activado para confirmar que nada mecánico se altera. (Verificado con Playwright, `reducedMotion: 'reduce'` real del navegador: nivel 12 sube la barra igual pulsación a pulsación; una victoria real en el nivel 1 muestra la modal de "Cookies Accepted" a los ~150ms de pulsar Agree, en vez de esperar a la animación de ~800ms; el contenedor del confeti tiene `display: none` computado y ninguna pieza se pinta en pantalla.)

## G · Robustez

- [x] Error boundary alrededor del nivel activo: si un chunk falla al cargar, mostrar una `XPDialog` de error con botón de volver a la selección; probarlo forzando el fallo del `import()`. (As-built: `LevelErrorBoundary.tsx` — límite de error de clase envuelto en un componente funcional que le pasa `t()` ya resuelto por props (los límites de error no pueden ser hooks). Envuelve solo el `Suspense`/`LevelComponent` dentro de `LevelHost`, no toda la ventana: la barra de título y la X siguen ahí detrás. Nuevo desenlace `{ outcome: 'error' }` en `LevelExitResult`; `AppShell` lo resuelve con `abandonLevel()` —nueva acción de `runStore`— en vez de `resetRun()`: un hipo de red no le cuesta el progreso al jugador. Test unitario forzando `lazy(() => Promise.reject(...))`; verificado además en el build real de producción (`vite preview`) abortando la petición de red del chunk de un nivel — el diálogo aparece con un único botón y vuelve a selección correctamente.)
- [x] Reverificar el arranque con `localStorage` vacío, corrupto y con datos de esquema antiguo. (Verificado con Playwright contra `npm run dev`: las tres situaciones arrancan sin `pageerror`. De paso, `playerStore.ts` se alineó con el mismo patrón defensivo que `runStore`/`settingsStore` (`{ ...DEFAULT, ...load(...) }`, no solo `...load(...)`) — sin bug activo hoy, pero cierra una inconsistencia real de cara a futuros campos.)

## H · Cierre del proyecto

- [ ] Grabar el GIF de demo (partida real, unos segundos de varios niveles: rotación del 3, plinko del 4, tablero del 6).
- [ ] Escribir el `README.md` en español con las doce secciones acordadas (ver `plan.md`, paso 10), con la voz *cute* del proyecto: corazón pixel y kaomoji como separadores, sin recargar.
- [ ] ✋ **Sofía**: aprobar el texto de "Por qué lo construí" y el de "Cómo se construyó (SDD + Claude Code)" — son las dos secciones que hablan de ella, no del juego.
- [ ] Metadatos en `index.html`: `<title>`, descripción y Open Graph con imagen; verificar cómo se ve el enlace compartido.
- [ ] Sincronización documental final: GDD, constitución y las 17 features coherentes con lo construido; todas las casillas de checkpoint cerradas; roadmap con todo en "Hecho".
- [ ] ✋ **Checkpoint final de Sofía**: partida completa en su móvil real vía Pages y visto bueno al README y a los metadatos.
- [ ] Mover la feature a "Hecho" en `../../constitution/roadmap.md`. **Fin del proyecto.**
