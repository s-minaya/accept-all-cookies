# 014 · Nivel 10 — Legitimate Interest — Tareas

- [x] Añadir la ranura `overlay` al canal nivel→host (`useLevelOverlay`, `useLayoutEffect`, render a pantalla completa sobre la ventana en `LevelHost`) + tests; verificar que los niveles 1–9 siguen en verde. (De paso, ganó también `titleBarRef` — ref de solo lectura a la barra de título real del host, `useHostTitleBarRef()` — para que el nivel pueda enganchar `usePointer` SOLO sobre la barra de título, no sobre toda la ventana como el nivel 3; requería `forwardRef` en `TitleBar` y una nueva prop `titleBarRef` en `XPWindow`.)
- [x] Documentar la ranura `overlay` en `tech-stack.md` y AGENTS.md (junto con la lección ya conocida: el componente que posiciona debe ser el que se monta). (`windowTransform` generalizada de paso: antes solo aceptaba grados de rotación (`useWindowRotation` publicaba `${deg}deg` y `LevelHost` lo envolvía en `rotate()`); ahora la ranura guarda el valor CSS de `transform` ya resuelto — `useWindowRotation` publica `rotate(${deg}deg)`, el nuevo `useWindowTranslation(x, y)` publica `translate(${x}px, ${y}px)` — necesario para que la ventana nº 1 pueda trasladarse por la misma ranura que usa el nivel 3 para rotar, tal y como pedía el plan.)
- [x] Implementar `windows.ts` (modelo puro: duplicación una vez por ventana, tope de 7, clamping al viewport, sorteo del Agree con semilla) + tests.
- [x] Implementar `LevelWindow.tsx` + `renderFooter(windowId, isAgree)` como **única** ruta de render de una ventana de este nivel (usada tanto por la nº 1 como por las copias). (Precisión sobre el plan: `LevelWindow` renderiza las copias 2-7; la nº 1 sigue viviendo en el `XPWindow` real de `LevelHost` — como dice la arquitectura de `013-spec.md` — pero ambas comparten literalmente la misma función `renderFooter`, así que el pie nunca puede divergir entre ellas.)
- [x] Mover la ventana nº 1 publicando `translate(...)` en la ranura `windowTransform`; publicar su pie con `useLevelFooter` usando el mismo `renderFooter`.
- [x] Renderizar las copias 2–7 en `overlay`, posicionadas por custom property escrita por ref (sin re-render por frame).
- [x] Arrastre por la barra de título con `usePointer`: al confirmarse el drag, nace la copia (si procede) en la posición inicial de la ventana arrastrada.
- [x] Al llegar a la séptima ventana, sortear cuál lleva el `Agree` y cambiar solo ese botón, sin animación.
- [x] `paused` congela arrastres e input; cleanup total al desmontar + test de no-fugas.
- [x] Sustituir el hueco 10 del registro; verificar chunk propio sin matter.js.
- [x] Añadir `levels.10.*` a ambos diccionarios.
- [x] GDD §14: tope de ventanas y nota de dónde nace la copia.
- [x] GDD §15.2, ventana reducida en móvil: `XPWindow` gana la prop `compactOnMobile` (modificador `&--compact-mobile`, `width: min(72vw, var(--xp-window-max-width))`, umbral `≤480px` — ver nota de la tercera ronda más abajo, subido desde `≤375px`), pedida al revisar el checkpoint — el criterio de aceptación de esta spec no lo exigía (solo pedía que las 7 siguieran siendo alcanzables a tamaño real), pero el GDD sí, y prevaleció el GDD. `LevelDefinition` gana `compactWindowOnMobile` para que `LevelHost` se lo pase a la ventana nº 1; `LevelWindow` lo pasa siempre a las copias. Verificado con Playwright: sin scroll horizontal con varias ventanas solapadas, botones del pie siguen midiendo ≥44px.
- [x] Revisar el CSS de `XPWindow` en busca de suposiciones de "una sola ventana en la página" (selectores de contexto, ids, `z-index` frente a la modal de fin de nivel). (Sin problemas: `XPWindow`/`TitleBar` no usan ids ni selectores dependientes de contexto; la modal de fin sigue ganando en z-order por ser hermana posterior de `.level-host` en el DOM, igual que `.level-host__overlay` — verificado jugando una partida completa con 7 ventanas en pantalla.)
- [x] Primera ronda de correcciones tras jugar la primera versión (4 cambios, ver blockquote de `014-plan.md`):
  1. **La ventana agarrada pasa al frente** ("queremos que se arrastre la pantalla de delante y deje de ocultar la de atrás"): `WindowState` gana `zIndex`; `bringToFront(windows, id)` lo sube por encima del máximo actual, llamado en CADA arrastre (`handleHostDragStart`/`handleCopyDragStart`), ocurra o no una duplicación (agarrar una ventana ya parida también debe traerla al frente — cubierto con test). La copia nueva nace por DEBAJO de quien la parió (`spawnFrom`, un `zIndex` más bajo), coherente con "se despega y deja otra detrás". Ranura nueva del canal, `windowZIndex` (+ `useWindowZIndex`), para que la nº 1 publique el suyo igual que las copias — sin ella, la nº 1 nunca podría subir por encima del `overlay` (limitación real de la arquitectura, señalada y descartada en el plan original; superada con esta ranura en vez de con un cambio estructural). `--window-z-index` en el rotador (`LevelHost.module.scss`) y `--window-z` en cada copia (`Level10.module.scss`) compiten en el mismo espacio numérico porque ni `.level-host` ni `.level-host__overlay` crean su propio contexto de apilado. Efecto colateral necesario: el botón dev (`z-index:10`) y la modal de fin (`XPDialog`, sin `z-index` explícito antes) subieron a 1000/999 — con `zIndex` de ventana creciendo sin tope a lo largo de la partida, el 10 original ya no era margen de sobra.
  2. **La séptima ventana nunca lleva el Agree**: `agreeWindowId` excluye `Math.max(...ids)` (la recién nacida) antes de llamar a `pickAgreeWindow`; `windows.ts` no cambia de firma, el filtro vive en `Level10.tsx`.
  3. **Asomo parcial en `xs`**: `clampToViewport` gana un 4º parámetro opcional `minVisibleX` (por defecto `size.width` = contención total, sin cambiar a nadie que no lo pase); `minVisibleXFor(width)` en `windows.ts` decide 30% del ancho en `≤375px`, 100% el resto — usado tanto por la nº 1 (`Level10.tsx`) como por las copias (`LevelWindow.tsx`).
  4. **Sin fondo blanco en el marco azul**: `.level-10` pierde su `background: var(--color-white)` propio; se deja ver el beige de `.xp-window__interior`.
- [x] Tercera ronda de correcciones, jugando la versión con las dos anteriores ya aplicadas (ver blockquote de `014-plan.md`):
  1. **El botón derecho también es rojo/Disagree hasta el sorteo** — la ronda 1 lo había dejado siempre verde de fondo, con solo el texto cambiando (lectura equivocada del GDD/`Estado inicial`, que YA decía "verde, pero también Disagree" — la ronda 1 leyó ese "verde" como el color base en vez de como parte del chiste). Arreglado en `renderFooter` (`Level10.tsx`): `variant={isAgree ? 'agree' : 'disagree'}` en vez de `variant="agree"` fijo. Ambos botones son ahora indistinguibles (mismo color, mismo texto) hasta que el sorteo cambia a la vez color y texto de uno solo. Test nuevo: los 14 botones son `xp-button--disagree` antes del sorteo; el único `Agree` tras el sorteo es también el único `xp-button--agree`.
  2. **El umbral "móvil" (asomo parcial + ventana compacta) estaba en `xs` exactamente (≤375px), y eso NO bastaba**: probado en un móvil real, "al arrastrar una pantalla no te deja arrastrar la siguiente" y "no se esconden parcialmente". Diagnóstico: la ronda de QA anterior verificó "móvil" con `page.mouse` de Playwright a un viewport estrecho — **`page.mouse` genera eventos de RATÓN siempre, incluso con `hasTouch:true` en el contexto**, así que nunca ejercitó un toque de dedo de verdad ni pudo detectar el problema. Repetido con toques reales (`Input.dispatchTouchEvent` por CDP, `page.context().newCDPSession(page)`) a 375/390/430px: a 375px un arrastre normal de dedo funcionaba, pero a 390px y 430px (el rango real de casi cualquier teléfono: iPhone 12-16 miden 390-430px) la ventana se quedaba a tamaño casi completo y con contención total — un arrastre de dedo normal no la alejaba lo bastante como para dejar de tapar a la copia nacida debajo, confirmando el mismo síntoma reportado. `MOBILE_BREAKPOINT_PX` sube de 375 a 480 (`windows.ts`, cubre `xs`+`sm` enteros) y el `@media` de `XPWindow.module.scss` (`&--compact-mobile`) igual. Reverificado con toques reales a 375/390/430px: 375 y 390 quedan perfectos con un arrastre normal (~150px); 430 (el extremo más ancho de "sm") necesita un arrastre algo más generoso, coherente con que la ventana también es un poco más ancha ahí — no se consideró necesario perseguir ese caso límite reduciendo más el `72vw` (los dos botones del pie ya rondan el mínimo de ancho que necesitan).
- [x] Cuarta ronda de correcciones (ver blockquote de `014-plan.md`): el punto 4 de la primera ronda ("sin fondo blanco") fue un malentendido — la corrección pedía lo contrario: el recuadro **siempre** fue blanco, como en los niveles 1/2/7/8/9 (todos con `background: var(--color-white)` en su propio wrapper), y así debía seguir. Revertido en `Level10.module.scss`. De paso salió a la luz un bug real independiente: las copias 2-7 renderizaban el texto con un `<p>` suelto sin clase (sin fondo, sin tipografía) mientras la nº 1 sí tenía el bloque estilado completo (`.level-10`/`.level-10__text`) — una diferencia visual real que delataba cuál era la original, exactamente lo que la "única ruta de render" de este nivel debía evitar. Arreglado extrayendo `consentContent` (`Level10.tsx`, memoizado por `consentText`) y reutilizando el MISMO nodo React tanto en el `return` del componente como en `children` de cada `LevelWindow` — no dos JSX que puedan desincronizarse, literalmente el mismo elemento. Test nuevo: el texto de una copia vive dentro de un contenedor con clase `level-10` (no un `<p>` suelto) y su contenido coincide con el de la nº 1.
- [x] ✋ **Checkpoint**: aprobado tras cuatro rondas de correcciones — la duplicación con la ventana agarrada pasando al frente, la nº 1 indistinguible de las copias (fondo blanco y tipografía del texto incluidos) y el caos de siete ventanas en móvil (con asomo parcial), todo verificado jugando cada ronda.
- [x] QA: duplicar hasta 7 y comprobar que no salen más; ganar pulsando el Agree; perder por Disagree de una copia, por la X de una copia y por contador; comparar el marcado de la nº 1 con el de una copia; `paused` a medio arrastre; recarga a mitad (una sola ventana) y con desenlace pendiente; 5 anchos con todas las ventanas alcanzables (verificado con Playwright: estado inicial, arrastre→duplicado, una copia por ventana, tope de 7, sorteo del Agree, victoria, las tres derrotas, recarga a mitad, contador e indistinguibilidad compartidos, y los 5 anchos sin scroll horizontal). Segunda ronda de QA tras la corrección del apilado: al llegar a 7, para pulsar el Agree hace falta despejar primero cualquier ventana nacida DESPUÉS que la tape (nunca antes, esas ya no pueden estar por encima) — verificado repartiendo las 7 en una rejilla amplia con Playwright antes de pulsar; comportamiento esperado, no un bug (mismo razonamiento que un gestor de ventanas real). Tercera ronda de QA (móvil con toques reales por CDP, no ratón): duplicar y volver a duplicar la copia con el dedo a 375/390/430px, asomo parcial al arrastrar muy lejos, botones rojos/verde correctos — ver el punto de la corrección más arriba para el detalle.
- [x] Validar contra los criterios de aceptación de `spec.md`.
- [x] Mover la feature a "Hecho" en `../../constitution/roadmap.md`.
