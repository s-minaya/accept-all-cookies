# 013 · Nivel 9 — Fingerprinting — Tareas

- [x] Implementar `cell.ts` (máquina pura por casilla: vacía → visible → congelada; congelar arma la casilla si está vacía; descongelar la expone al ciclo siguiente) + tests. (Corregido sobre la primera versión: la máquina original tenía cuatro fases con un campo de progreso (`vacía → subiendo → congelada → saliendo`, un recorrido continuo por casilla) — el diseño real no tiene movimiento ni progreso, los botones aparecen y desaparecen directamente. Máquina simplificada a tres fases sin campo de progreso; ver la nota al principio de `013-plan.md`.)
- [x] Implementar el generador de ciclos (`cycle.ts`, sustituye a `spawner.ts`+`gridStep.ts` de la primera versión): cada ciclo elige varias casillas elegibles a la vez (`SIMULTANEOUS_COUNT` de 12) y les reparte tipo al 50/50 con al menos un Agree garantizado en el lote — `chooseCycleBatch` + `runCycle`, con tests incluida una comprobación de la garantía sobre 100 semillas distintas.
- [x] Implementar el reloj único sobre rAF (delta acumulado, congelable por `paused`, cancelable) + test con tiempo simulado. (`clock.ts`, sin cambios respecto a la primera versión — sigue siendo un simple entregador de delta en ms; el acumulador de "cuándo toca el siguiente ciclo" vive ahora en `Level09Grid.tsx`, no en el reloj.)
- [x] Detección de inmovilidad sobre `usePointer` (extendiendo el hook si hace falta, nunca reimplementándolo en el nivel): quieto ~1 s congela la casilla apuntada; movimiento > 8 px descongela. (El hook SÍ hacía falta extenderlo: antes de esta feature, `stillnessMs` solo funcionaba con el puntero pulsado y releía el punto FIJO de inicio del gesto, no el punto vivo — no servía para "ratón quieto sin pulsar". Añadidos `pointerenter`/`pointerleave` como arranque/parada del seguimiento (funciona igual para ratón en hover y para dedo en contacto, sin distinguir `pointerType` a mano) y un nuevo callback `onUnstill`. Segundo bug real encontrado con Playwright simulando un toque mantenido: en táctil, levantar el dedo SIN pulsar nada solo paraba el bucle interno del hook sin avisar — la casilla se quedaba congelada para siempre y, peor, bloqueada para volver a congelar ninguna otra (`frozenIndexRef` nunca se soltaba). Arreglado: soltar el contacto/hover, por la razón que sea, cierra el episodio de quietud en curso y avisa con `onUnstill` si estaba activo. Cubierto en `usePointer.test.tsx`; regresión verificada en los niveles 3/4/7 que ya usaban el hook.)
- [x] Equivalente táctil: mantener pulsado congela; `pointerup` sobre botón congelado = pulsarlo; `pointerup` sin congelado = nada; arrastrar fuera antes de levantar = nada. Ruta de acción única (sin doble disparo) + test. (Sale gratis: los 12 botones son `XPButton` reales con `onClick` nativo.)
- [x] Implementar `Level09.tsx` (texto en el marco, publica la cuadrícula) y `Level09Grid.tsx` (12 casillas del mismo tamaño que un botón real; el componente que mide/posiciona es el que se monta). (Corregido: la primera versión posicionaba los botones por custom property escrita por ref en cada fotograma, sobre una casilla más alta que el propio botón, con un recorrido interno. El diseño real no tiene nada que escribir por fotograma — los ciclos son eventos discretos (~2/s), así que las 12 casillas viven en estado de React normal, sin ningún escritor imperativo.)
- [x] Estilos `Level09.module.scss`: rejilla 4×3 (md+) / 3×4 (xs/sm), casilla del mismo tamaño que un `XPButton` real (`3rem` de alto × `3rem × 2.3` de ancho), área táctil ≥ 44 px, fade rápido de aparición.
- [x] `paused` congela los ciclos, el temporizador de inmovilidad e input; cleanup total al desmontar + test de no-fugas.
- [x] Sustituir el hueco 9 del registro; verificar chunk propio sin matter.js.
- [x] Añadir `levels.9.*` a ambos diccionarios.
- [x] GDD: §14 con los parámetros nuevos (duración de un ciclo, casillas simultáneas por ciclo, tiempo de inmovilidad) y §15.2 precisando el equivalente táctil final. (De paso, corregida la sección "Animación de los botones"/"Escenario"/"Victoria y derrota" del GDD para reflejar los ciclos sincronizados en vez de la subida independiente por casilla de la primera versión.)
- [x] ✋ **Checkpoint**: mecánica corregida (casillas del tamaño real de un botón, ciclos sincronizados con varios botones a la vez y al menos un Agree garantizado) aprobada. Sin comentarios sobre el ritmo de los ciclos, el tiempo de inmovilidad, la lectura del botón congelado con el dedo encima, ni sonido al aparecer/congelar — se mantienen los valores por defecto (sin sonido).
- [x] QA: ganar con un Agree congelado (verificado con Playwright, seed conocida); perder con un Disagree del lote (verificado); solo se congela la casilla apuntada, las otras 11 siguen ciclando (verificado, test determinista de dos ciclos consecutivos); descongelar al mover expone la casilla al siguiente ciclo (verificado); `paused` con una casilla congelada y con el grid entero (test con tiempo simulado); recarga a mitad (cuadrícula vacía de nuevo, verificado); 5 anchos sin scroll con casillas de tamaño real de botón (bien por encima del mínimo de 44 px).
- [x] Validar contra los criterios de aceptación de `spec.md`.
- [x] Mover la feature a "Hecho" en `../../constitution/roadmap.md`.
