# 016 · Nivel 12 — Accept All + créditos — Tareas

## Nivel 12

- [x] Implementar `acceptAll.ts` (máquina pura: progreso, clics, fases `agree`/`trap`/`restored`, decaimiento, `switchAt` en [15,35] con el PRNG compartido) + tests (series largas sin llegar al 100 %, cambio exacto en `switchAt`, derrota en `trap`, restauración a los 2 s, pulsación justo en el límite en ambos sentidos). (As-built: bug real encontrado por los propios tests — `sinceLastClick` se reducía por módulo tras cada paso de decaimiento, lo que le impedía acumular nunca los 2 s completos con deltas de rAF reales (~16 ms, muchísimos ticks); corregido para que crezca sin reducirse hasta que un clic lo resetea, comparando la división entera antes/después para saber cuántos pasos de decaimiento son NUEVOS en cada tick. Añadido un test específico de "muchos ticks pequeños vs. un tick grande" para que no vuelva a colarse.)
- [x] Conectar el reloj rAF de la casa a `tick(dt)`; congelable por `paused`, cancelable en cleanup. (As-built: vive en `ProgressBar.tsx`, el único componente con bucle propio — el pie en `Level12.tsx` solo lee/escribe el mismo `modelRef` al pulsar.)
- [x] Implementar `Level12.tsx`: consentimiento en el marco (nodo memoizado); barra vía `useLevelBoard` (relleno por custom property escrita por ref, componente propio que se monta con el nodo); pie vía `useLevelFooter` con el Disagree fijo y el botón protagonista (variante y texto derivados de la fase, **sin `transition` de color**). (As-built: `fillRef` se crea en `Level12.tsx` y se pasa a `ProgressBar` — el pie también escribe en él al pulsar, para que el relleno reaccione al instante del clic en vez de esperar al siguiente fotograma del reloj.)
- [x] Victoria: al pulsar en `restored`, bloquear input, animar la barra al 100 % (~400 ms) y llamar a `onWin()`.
- [x] `touch-action: manipulation` en los botones (clic compulsivo en móvil sin zoom por doble toque). (Ya lo tenía `XPButton` de fábrica, GDD-wide — verificado, no hizo falta CSS nuevo.)
- [x] Sustituir el hueco 12 del registro; verificar chunk propio sin matter.js.
- [x] Añadir `levels.12.*` a ambos diccionarios.

## Créditos

- [x] Implementar `CreditsScreen` (XPWindow sin contador ni X, `scrollableContent`, botón de volver) sustituyendo el placeholder del hueco `credits` de `AppShell`. (As-built: pasa de archivo suelto a carpeta propia `CreditsScreen/`, igual que `LevelSelectScreen/`; `PlaceholderScreen` se queda sin ningún consumidor y se borra entero.)
- [x] Redactar `credits.*` en ES y EN a partir del GDD §10: felicitación troll, autoría (Sofía Minaya), inspiración (*Doki Doki Action Game*), homenaje ("Sans © Toby Fox (Undertale) — homenaje sin ánimo de lucro") y el remate del localStorage. (As-built: namespace `credits.*` propio, no `shell.credits.*` — ver AGENTS.md. Las líneas "Game design ... Sofía Minaya" del GDD se resuelven con una guía punteada por CSS/flexbox, no con puntos literales en el texto, para que funcione igual en cualquier idioma.)
- [x] Cablear el botón `Credits` de la modal de victoria del nivel 12 para navegar a `credits` en vez de a la selección.
- [x] El botón de volver de los créditos llama a `resetRun()` y navega a la landing; el ranking conserva el récord con su marca `finished` (test).
- [x] **As-built (checkpoint, ronda 2 — reescritura de texto)**: Sofía pidió sustituir el texto de los créditos manteniendo la guía de puntos y la estructura (intro en dos tiempos "Vaya. Lo has conseguido." / "De verdad.", cuatro roles con "Sofía Minaya & Claude" en Código —crédito compartido explícito, sin trasladar su mensaje personal literal—, fila "Aparición especial" para Sans/Toby Fox, "Agradecimiento especial" en tres bloques y el chiste de los datos ampliado a 7 líneas con remate final "Tampoco has leído esta parte."). `credits.*` reescrito entero en ambos diccionarios; verificado sin overflow horizontal a 1280/390/375px (Playwright) y sin claves JSON duplicadas tras el reemplazo.
- [x] **As-built (checkpoint, ronda 2 — confeti)**: petición de Sofía ("¿puedes poner el efecto de confeti al completar el último nivel y salir los créditos?"), floteando la idea de un paquete npm. Se preguntó explícitamente (regla de AGENTS.md: ninguna dependencia nueva sin actualizar antes `tech-stack.md`) y se optó por CSS puro, mismo patrón sin librería que `GiantVerdict`: `Confetti.tsx` genera 60 piezas una sola vez (`useMemo`), cada una un cuadradito de 8×8px con la paleta de tokens ya existente (`--color-agree-bg/border`, `--color-disagree-bg/border`, `--color-title-gradient-start`, `--color-cute-bg` — ningún color nuevo); caída y desvanecido con `@keyframes` + `animation-fill-mode: forwards`, sin `setTimeout` para limpieza. `aria-hidden`, `pointer-events: none`, `z-index` por debajo de cualquier interacción real — nunca bloquea el botón de volver (test + verificado con Playwright).
- [x] ✋ **Checkpoint con Sofía**: textos de los créditos en ambos idiomas (es su voz), ritmo de la barra y si la trampa cae bien jugando del tirón tras los once niveles anteriores. (Aprobado: Sofía retocó el texto final a mano — kaomojis en `intro2`/`specialThanksTo`/`dataJoke7`, "Pinky promise."/"Prometido." al final de `dataJoke5`, "Sofía Minaya & Claude Code" en el crédito de Código en ES —asimetría deliberada con el "Sofía Minaya & Claude" del EN—. Tests de `CreditsScreen` actualizados a comprobación por regex donde el texto exacto ya no aplica.)

## Cierre

- [x] GDD: §14 con los parámetros nuevos (incremento por clic, decaimiento e intervalo, rango de `switchAt`, tiempo de restauración, duración del llenado final) y §10 con el crédito a Toby Fox junto al de Doki Doki.
- [x] `paused` congela decaimiento, temporizador de 2 s e input; cleanup total al desmontar + test de no-fugas.
- [x] QA: ganar parando a tiempo; perder cayendo en la trampa, por el Disagree fijo, por contador y por X; **recorrido completo landing → 12 niveles (con `?dev` para los previos) → créditos → landing → Empezar y comprobar que arranca una partida nueva desde el Nivel 1**; el ranking marca `finished` aunque no se lleguen a ver los créditos; `paused`; recarga a mitad; 5 anchos; móvil real con **toques CDP** (`Input.dispatchTouchEvent`, no `page.mouse`). (As-built: verificado en 1280px escritorio y 390×844/375×667 móvil vía CDP — sin desbordamiento horizontal ni en el nivel ni en los créditos, botón protagonista ≥44px. Contador/X heredados de `LevelHost` sin que `Level12` los intercepte, mismo mecanismo genérico que el resto de niveles. Lección de la 014 aplicada de nuevo: un `page.waitForTimeout` largo y único en Chromium headless puede ralentizar rAF de forma artificial — sondear en pasos cortos lo evita; no ocurre con un usuario real.)
- [x] Validar contra los criterios de aceptación de `spec.md`.
- [x] Mover la feature a "Hecho" en `../../constitution/roadmap.md`.
