# 007 · Nivel 3 — Personalization — Tareas

**Estado:** en curso

- [x] Refactor del canal nivel→host (`hostChannel.ts` con ranuras `footer`, `windowTransform` y `windowRef`; `useLevelFooter` como envoltorio; nuevos `useWindowRotation`/`useHostWindowRef`) + tests; verificado que los tests de los niveles 1–2 siguen en verde (solo cambió el import de sus arneses de test, no su código).
- [x] Documentar el canal consolidado y la excepción de timers (rAF/Runner con limpieza obligatoria; `setInterval` sigue prohibido) en `tech-stack.md` y AGENTS.md. También matter.js como dependencia instalada y el `forwardRef` nuevo de `XPButton`.
- [x] Implementar `rotationLogic.ts` (ángulo desde el centro, delta angular con normalización ±180°, rotación acumulada) + tests. El tap-vs-drag no se reimplementa: lo resuelve `usePointer` reutilizando `pointerLogic` tal cual.
- [x] **Corregido tras revisión de Sofía, primera ronda** (la primera versión anclaba el Agree fuera del viewport con una geometría por viewport en `agreeAnchor.ts` — descartada por completo, archivo y test eliminados): el Agree es un cuerpo físico más dentro de `rain.ts`, oculto en una cámara fija a la derecha del recuadro de lluvia, recortada por su propio `overflow: hidden`. Sin función de geometría: el mecanismo sale de la gravedad ya orientada a pantalla (solo el giro antihorario le da a esa gravedad la componente horizontal que lo saca de su escondite).
- [x] **Corregido tras revisión de Sofía, segunda ronda** (tras probarlo en vivo): cuatro ajustes más.
  1. **Layout**: el texto de consentimiento va dentro del marco azul del área de juego (excepción del GDD §4.3, como los niveles 1-2), no en un recuadro de consentimiento pequeño separado — se quitó `consentKey` del registro y `Level03.tsx` renderiza su propio `<p>` igual que `Level01.tsx`. Nuevo campo opcional `LevelDefinition.fillHeight` (`types.ts`, reenviado por `LevelHost` a `XPWindow`): el nivel 3 lo activa porque texto + recuadro de lluvia no caben en el límite moderado por defecto (~12rem) — sin él, el recuadro de lluvia quedaba recortado/fuera de la vista (bug real, detectado en QA con Playwright tras el primer intento de este mismo ajuste).
  2. **Sin flash al cargar**: el Agree (y los Disagree) nacen ya con su posición sincronizada de forma síncrona (`Level03.tsx` crea la simulación en `useLayoutEffect`, no `useEffect`; `rain.ts` llama a `syncButtonPosition` una vez para cada cuerpo antes de devolver el control) — antes se veía un fotograma en (0,0) antes de que el motor de física arrancara.
  3. **El Agree nace `isStatic`** en su posición de reposo dentro de la cámara oculta (quieto, sin caída animada) y se libera (`Body.setStatic(..., false)`) la primera vez que la ventana gira, en cualquier sentido.
  4. **Sellado tras la primera revelación**: una pared "sello" (creada pero no añadida al mundo hasta entonces) se añade la primera vez que el Agree cruza por completo al recuadro visible, para que no pueda volver a la cámara oculta aunque el jugador gire después hacia el otro lado.
- [x] **Corregido tras revisión de Sofía, tercera ronda** ("el borde azul oscuro SOLO cubre los términos y condiciones de las cookies. El tablero de juego se renderiza justo debajo, aprovechando el tamaño sobrante que queda"): el texto y el recuadro de lluvia dejan de compartir un mismo marco azul estirado (`fillHeight` sobre ambos, ronda anterior). Nueva ranura `board` en el canal nivel→host (`hostChannel.ts`: `setBoard`/`useLevelBoard`, con `useLayoutEffect` para no reintroducir el flash de carga) y nueva prop `boardBelowFrame` en `XPWindow`: el nivel publica su tablero aparte, `XPWindow` lo renderiza DEBAJO del marco azul, fuera de él, y el marco (`children`, ahora solo el texto) deja de estirarse (`flex: 0 0 auto` en vez de `flex: 1` vía el modificador `.xp-window--has-board`). `Level03.tsx` cambia su `useRef` de contenedor por un `useState` (callback ref): el recuadro ahora se monta en un punto del árbol ajeno al propio `Level03` (lo renderiza `XPWindow`, vía el canal), así que el efecto que crea la simulación de matter.js debe reaccionar a que el nodo exista, no solo a que el componente se monte.
- [x] Implementar `Level03.tsx`: texto dentro del marco azul (ajustado a su contenido) + recuadro de lluvia publicado aparte (`useLevelBoard`) y renderizado por `XPWindow` debajo de ese marco (`fillHeight` sigue activo: no da altura al texto, sino espacio "sobrante" real al tablero); rotación con `usePointer` sobre `windowRef` publicando `useWindowRotation`; pie con solo Disagree. El Agree se renderiza dentro del propio recuadro de lluvia (último en el DOM, para pintar por encima del montón de Disagree en cuanto entre en vista).
- [x] Implementar `rain.ts` con matter.js dinámico: spawn continuo, reciclaje, población ≤25 en escritorio / ≤12 en dispositivos táctiles (parámetro), gravedad en coordenadas de pantalla (test del vector), sync cuerpo→DOM por `afterUpdate` de `Runner` (rAF interno de matter.js, no uno propio); cuerpo adicional para el Agree (nace `isStatic`, se libera con el primer giro, se sella tras la primera revelación), en la cámara oculta (sin pared separadora respecto al recuadro visible hasta el sello: mismo espacio físico continuo).
- [x] Cleanup completo al desmontar (Runner parado, listener desconectado, Engine destruido) + test de no-fugas; `paused` congela física, rotación e input.
- [x] Cursor `grab`/`grabbing` sobre la ventana mientras se puede/está arrastrando (vía `:active`, sin estado de React).
- [x] Extraer `isCoarsePointerDevice()` a `src/hooks/device.ts` (antes vivía privada dentro de `AudioManager.ts`) y reutilizarla en el nivel 3 para reducir la población de la lluvia en dispositivos táctiles (12 en vez de 25 — no dejan sitio en pantalla, corrección de Sofía).
- [x] Sustituir el hueco 3 del registro; verificado chunk propio (`Level03-*.js`, ~90 kB) y que matter.js no aparece en el bundle principal ni en los chunks de otros niveles (`grep` sobre `dist/`).
- [x] Añadir `levels.3.*` a ambos diccionarios.
- [x] Añadir al GDD §14 los parámetros nuevos de este nivel (población máx. de la lluvia: 25 escritorio / 12 táctil, tasa de spawn: 350 ms, tiempo de reposo antes de reciclar: 4 s, ancho de la cámara oculta del Agree: 140 px).
- [ ] ✋ **Checkpoint con Sofía**: tacto de la rotación, densidad de la lluvia, tamaño táctil de los Disagrees en su móvil, y si el escondite del Agree (solo revelado por el giro antihorario) se siente descubrible o hace falta alguna pista adicional; decidir si hace falta inercia. Pendiente — solo lo puede juzgar Sofía con las manos encima.
- [x] QA con Playwright contra el servidor de desarrollo (real, no jsdom), dos rondas: partida entera ganando (girar a la izquierda hasta que el Agree rueda a la vista, pulsarlo, categoría correcta) y perdiendo por el Disagree fijo y por uno de la lluvia; contador y X funcionando con la ventana rotada; un arrastre que empieza sobre el Disagree fijo rota en vez de perder; recarga a mitad de nivel (rotación vuelve a 0°, lluvia y Agree nuevos, sigue jugable); Agree confirmado oculto y quieto a 0°, sin movimiento hasta el primer giro, revelado solo por el giro antihorario (el horario no lo revela) y confirmado que ya no se esconde tras revelarse aunque se gire al revés; cursor `grab` confirmado; texto dentro del marco azul con el recuadro de lluvia visible sin scroll; población reducida a 12 confirmada emulando un dispositivo táctil. Cinco bugs reales encontrados y corregidos en el proceso (ver `007-plan.md`/comentarios en el código): un techo físico bloqueaba la lluvia antes de entrar en el recuadro; `usePointer` capturaba el puntero en `pointerdown` en vez de al confirmarse un arrastre, lo que retargetaba el `click` nativo del navegador e impedía pulsar cualquier botón dentro de la ventana rotable (corregido de forma general en el hook, no solo para este nivel); el Agree pintaba por debajo del montón de Disagree al revelarse (corregido con el orden del DOM); un cuerpo nacido `isStatic` por opción del constructor se quedaba con masa `Infinity` para siempre al reactivarlo, dando `NaN` bajo gravedad (corregido creándolo dinámico y marcándolo estático aparte, vía `Body.setStatic`); y el recuadro de lluvia quedaba recortado/fuera de la vista tras quitar `consentKey` (corregido con `fillHeight`). Los 5 anchos de referencia y el recorrido completo en móvil/tablet/escritorio reales, confirmados por Sofía. Tercera ronda (layout marco-solo-texto + tablero fuera): confirmado con Playwright que el marco azul se ajusta al texto, el recuadro de lluvia cae y se acumula en el espacio sobrante debajo de él, y que rotar (incluida la revelación del Agree) sigue funcionando igual tras mover el tablero a la nueva ranura `board` del canal. Cuarta ronda (ancho progresivo del recuadro de lluvia en tablet/escritorio, corrección de Sofía): confirmado con Playwright en los 5 anchos de referencia que el recuadro crece por escalón (`sm` 352px → `md` 416px → `lg` 480px → `xl` 544px) sin desbordar la ventana ni perder su ancho menor al del texto; a partir de `lg` la ventana ya no crece (tamaño fijo), así que el `width` porcentual también sube por breakpoint, no solo el `max-width` — si solo se sube el `max-width` no hay efecto visible una vez la ventana toca su propio tope (bug real, detectado en QA con Playwright: `xl` se quedaba en ~524px pese a pedir 576px).
- [x] Validar contra los criterios de aceptación de `spec.md`: todos cumplidos salvo el checkpoint de tacto (rotación, densidad, inercia), pendiente de Sofía; los anchos de referencia y el móvil/tablet/escritorio reales ya están confirmados.
- [ ] Mover la feature a "Hecho" en `../../constitution/roadmap.md` (pendiente del checkpoint de tacto: rotación, densidad de la lluvia, si el escondite del Agree se siente descubrible, si hace falta inercia).

## Mantenimiento (checklist recurrente)

- [ ] Cualquier cambio futuro en el canal nivel→host se re-valida con los niveles 1, 2 y 3 y con el nivel de prueba de la Playground.
