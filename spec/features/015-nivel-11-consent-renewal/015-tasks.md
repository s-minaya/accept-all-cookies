# 015 · Nivel 11 — Consent Renewal — Tareas

> Auditoría de traducción aplicada a todo el proyecto durante esta feature, no solo al nivel 11: `game.*` deja de ser "todo sin traducir en bloque" (solo Agree/Disagree/`cookiesAccepted` se quedan fijos, por su mecánica de similitud visual); Check/Stop/OK/Yes pasan a `es.json`. Además, varios nombres de nivel demasiado literales se revisaron: nivel 7 "Compartición de Datos" → "Uso Compartido de Datos"; nivel 8 "Proveedores de Terceros" → "Proveedores Externos"; nivel 6 "Seguimiento entre Sitios" → "Seguimiento entre Sitios Web"; nivel 10 "la provisión de" → "el suministro de"; nivel 2 "las personas visitantes" → "los usuarios". Ver `015-plan.md`/`015-spec.md` para el detalle.

- [x] Colocar los assets en `src/assets/`: `sans.png` (sprite) y `sans-voice.mp3` (~1 s, pensado para loop).
- [x] Implementar `conversation.ts` (guion de 8 pasos = 4 preguntas ×2, respuesta esperada por paso, `answer()` → advance/win/lose) + tests exhaustivos (las 8 posiciones, correcta e incorrecta en cada una).
- [x] Ampliar `AudioManager`: `startVoiceLoop()`/`stopVoiceLoop()` (elemento propio con `loop`, sujeto a `soundEffectsOn`) y `duckMusic(factor)`/`unduckMusic()` (multiplicador sobre los factores por dispositivo y el volumen de Ajustes, con fundido ~150 ms, idempotentes) + tests (loop arranca/para, el volumen vuelve exactamente al anterior, idempotencia).
- [x] Implementar `useTypewriter` sobre el reloj rAF de la casa (prefijo visible, `done`, `skip()`), arrancando voz + ducking al empezar cada frase y parándolos al terminar o al saltar; congelable por `paused` (voz pausada, música restaurada) y con cleanup que **siempre** deja la voz parada y la música restaurada + test con tiempo simulado. (As-built: la máquina de estados del reloj se extrajo pura a `typewriter.ts` — `tickTypewriter`/`skipTypewriter`/`isTypewriterUnlocked` — con `useTypewriter.ts` como envoltorio fino que la conecta a `useAudio()`; mismo patrón que `phaseClock.ts`/`cycle.ts` de niveles anteriores, testeado sin React en `typewriter.test.ts` y con `renderHook` en `useTypewriter.test.ts`.)
- [x] Implementar `Level11.tsx`: consentimiento en el marco (nodo memoizado); vía `useLevelBoard`, Sans abajo a la derecha + `SpeechBubble` con la pregunta y los botones `No`/`Yes` (`game.*`). Sin pie de ventana — mismo patrón sin pie que los niveles 8-9. (As-built: Sans + el guion viven en `Level11Board.tsx`, un componente propio que posee su propio `stepIndex` y `useTypewriter` — mismo motivo que `Level09Grid.tsx`, pero aquí no por el timing de montaje de `useLevelBoard`, sino para que el nodo publicado no cambie de identidad en cada letra escrita: `Level11Board` se re-renderiza por su cuenta, `Level11.tsx` lo publica una única vez por cada cambio de `paused`.)
- [x] Bloqueo de input: botones deshabilitados mientras escribe + margen `ANSWER_UNLOCK_MS`; `pointerdown` sobre el bocadillo completa el texto, corta la voz y restaura la música.
- [x] Estilos `Level11.module.scss`: bocadillo con cola hacia Sans; **Comic Sans solo dentro del bocadillo** (con fallback), nunca heredada por los botones ni por el resto del nivel; bocadillo a la izquierda en md+ y encima en xs/sm; ancho fluido para la pregunta larga; botones ≥ 44 px; sprite pixelado, más grande en xs/sm (10rem) que en md+ (8rem).
- [x] Sustituir el hueco 11 del registro; verificar chunk propio sin matter.js.
- [x] Añadir `levels.11.*` (consentimiento + 4 preguntas) a ambos diccionarios; `No`/`Yes` viven en `game.*` y se traducen con normalidad (ya no son excepción, solo Agree/Disagree/`cookiesAccepted` lo son).
- [x] Documentar `sans-voice.mp3` como quinto asset de audio (GDD §2.3, `tech-stack.md`, AGENTS.md) y añadir a GDD §14: ms/carácter, margen de desbloqueo, factor de ducking y duración del fundido.
- [x] Anotar para la feature 016 el crédito: "Sans © Toby Fox (Undertale) — homenaje sin ánimo de lucro", junto a la mención ya prevista a *Doki Doki Action Game*. (Recogido en GDD §13 y en la descripción del personaje, Nivel 11 "Escenario".)
- [x] `paused` congela escritura, voz e input y restaura la música; cleanup total al desmontar + test de no-fugas (**la música nunca se queda agachada**).
- [x] ✋ **Checkpoint**: aprobado tras los cambios de esta ronda — quitar el Disagree del pie, agrandar a Sans en móvil, traducir los botones neutros de `game.*` y una auditoría completa de `es.json` contra traducciones demasiado literales. Tamaño/posición de Sans y el bocadillo, Comic Sans en pantalla real, velocidad de escritura, volumen relativo voz/música, factor de ducking y la segunda vuelta jugada del tirón: todo aprobado sin más cambios.
- [x] QA: ganar las 8 respuestas; perder respondiendo mal (probado en la pregunta 1) y por X, ambos **a mitad de frase**; ráfaga de clics rápidos que no encadena respuestas; `skip()` completa y corta la voz (probado con toque real vía CDP); sin pie de ventana en ningún ancho (nada de Disagree ni Agree fuera del bocadillo, tras la corrección); Sans más grande en móvil sin desbordar; 1 ancho de escritorio (1280px) y uno móvil real (390×844, iPhone 390 — sin desbordamiento horizontal) con **toques CDP** (`Input.dispatchTouchEvent`, no `page.mouse`, lección de la 014); sin errores de consola durante la escritura; `sans-voice.mp3` se solicita de verdad al navegador (confirma que la voz intenta reproducirse). Las 8 posiciones de derrota, `paused` a mitad de frase, recarga a mitad y contador a 0 ya están cubiertas por `Level11.test.tsx`/`useTypewriter.test.tsx`, no repetidas en el navegador.
- [x] QA de la traducción de botones neutros (segunda ronda): verificado en el navegador que "Comprobar" (selección de niveles), "Aceptar" (diálogo de error del nivel 1) y "Parar" ×3 (nivel 5, el caso más ajustado — botones de 84px fijos alineados con los rodillos) se leen completos, sin desbordar ni cortarse.
- [x] Validar contra los criterios de aceptación de `spec.md`.
- [x] Mover la feature a "Hecho" en `../../constitution/roadmap.md`.
