import { Playground } from './playground/Playground'

/**
 * Until the game shell lands (feature 002), the Playground is the whole app:
 * it's what gets published to GitHub Pages.
 */
function App() {
  return <Playground />
}

export default App
